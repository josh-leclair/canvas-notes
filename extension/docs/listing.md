# Canvas Notes Web Clipper

Save web pages, selected passages, simplified articles, links, videos, and
images directly to the inbox of your self-hosted Canvas Notes installation.

Use the toolbar popup to preview the current page, add an optional note, and
choose what to clip, or right-click a page, rich-text selection, link, or image
for a one-step capture. YouTube
links become Canvas Notes video cards automatically. Every capture lands safely
in the inbox, ready to arrange on a canvas later.

The extension connects only to the Canvas Notes server URL you configure. It
sends data only after you invoke a clearly labeled clip command. It contains no
analytics, advertisements, passive page monitoring, or third-party cloud
service.

Requires a Canvas Notes server and a revocable API token created in Canvas Notes
Settings. Firefox 140 or newer is required on desktop; Firefox for Android 142
or newer is required on Android.

## Permissions

- **Access the active tab:** read only the page you explicitly choose to clip.
- **Context menus:** provide right-click clip commands.
- **Storage:** keep the configured server URL and API token locally in Firefox.
- **Access to a requested site:** connect to the Canvas Notes server you enter;
  image hosts are requested individually only when you choose “Clip image.”

## Suggested categories

Productivity; Bookmarks
